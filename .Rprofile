# Use Posit Public Package Manager so renv::restore() can find the
# historical package versions pinned in renv.lock (CRAN archives older
# versions over time; PPM keeps them).
options(repos = c(CRAN = "https://packagemanager.posit.co/cran/latest"))
source("renv/activate.R")
